"use client"

import Window from "@/components/Window";
import { useState } from "react";

const Sandbox = ({ id }) => {
  //
  // const { addStowedWindow } = useStow();
  // const [visible, setVisible] = useState(true);
  //
  //
  // const handleStow = () => {
  //   addStowedWindow({
  //     id: `unique-id-${id}`,
  //     title: `Sandbox Window ${id}`,
  //     onRestore: () => {
  //       setVisible(true);
  //     }
  //   })
  // }
  //
  // return visible && (
  //   <Window title="WOWOWO" onClose={() => { handleStow(); setVisible(false); }}>
  //     wpoeifjapoawijfapwoeijawpfeoijawpfoeijj
  //   </Window >
  // )
}

export default Sandbox
